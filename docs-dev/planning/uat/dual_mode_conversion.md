# 使用者驗收測試計畫 (UAT)

## 測試目標

驗證雙模式點字轉換架構在實際使用情境中是否正常運作，確保：

1. **內建模式** (InProcessBrailleConverter) 可正常轉換點字
2. **外部工具模式** (ExternalBrailleConverter) 可正常轉換點字
3. 兩種模式可透過組態檔順利切換
4. 轉換結果正確且一致

---

## 測試前準備

### 1. 建置應用程式

```powershell
# 建置 Debug 版本
dotnet build src\EasyBrailleEditApp\EasyBrailleEditApp.sln -c Debug

# 或建置 Release 版本
dotnet build src\EasyBrailleEditApp\EasyBrailleEditApp.sln -c Release
```

### 2. 啟動應用程式

#### 方式一：直接執行

```powershell
# 切換到輸出目錄
cd output\EasyBrailleEdit\Debug\net10.0-windows10.0.17763.0

# 執行應用程式
.\EasyBrailleEdit.exe
```

#### 方式二：使用 dotnet run

```powershell
cd src\EasyBrailleEditApp
dotnet run --project EasyBrailleEdit\EasyBrailleEdit.csproj --no-build -c Debug
```

### 3. 確認組態檔

**檔案名稱**：`AppConfig.ini`

**檔案位置**：

- Debug: `output\EasyBrailleEdit\Debug\net10.0-windows10.0.17763.0\AppConfig.ini`
- Release: `output\EasyBrailleEdit\Release\net10.0-windows10.0.17763.0\AppConfig.ini`

**說明**：首次執行時會自動產生，預設值為 `UseInProcessConversion=true`（內建模式）

### 4. 準備測試文字

建議準備以下測試內容：

- **簡單文字**：混合中英文，約 5-10 行
- **複雜文字**：包含標點符號、數字、特殊字元
- **大型文件**：100 行以上的文字檔案

---

## 測試案例

### 測試案例 1：內建模式基本轉換 ✅

**目的：** 驗證內建模式可正常轉換點字

**前置條件：**

1. 開啟 `AppConfig.ini`
2. 確認 `UseInProcessConversion=true`

**測試步驟：**

1. 啟動 EasyBrailleEdit 應用程式
2. 輸入或貼上測試文字（例如：「這是測試 Test 123」）
3. 點擊「轉換」按鈕
4. 觀察轉換過程

**預期結果：**

- ✅ 轉換成功完成
- ✅ 點字編輯器正常開啟並顯示轉換結果
- ✅ 沒有錯誤訊息
- ✅ 轉換後的點字符合預期（可與舊版本比對）

---

### 測試案例 2：外部工具模式基本轉換 ✅

**目的：** 驗證外部工具模式可正常轉換點字

**前置條件：**

1. 開啟 `AppConfig.ini`
2. 修改為 `UseInProcessConversion=false`
3. 重新啟動應用程式

**測試步驟：**

1. 啟動 EasyBrailleEdit 應用程式
2. 輸入或貼上**相同的**測試文字
3. 點擊「轉換」按鈕
4. 觀察轉換過程

**預期結果：**

- ✅ 轉換成功完成
- ✅ 點字編輯器正常開啟並顯示轉換結果
- ✅ 沒有錯誤訊息
- ✅ 轉換結果與測試案例 1 一致

---

### 測試案例 3：結果一致性比較 ✅

**目的：** 確認兩種模式產生的點字完全一致

**前置條件：**

1. 準備較長的測試文字檔案（建議 20-50 行）

**測試步驟：**

1. 使用**內建模式**轉換，儲存輸出檔案為 `output_inprocess.brl`
2. 切換到**外部工具模式**，轉換相同文字，儲存為 `output_external.brl`
3. 比較兩個檔案內容

**預期結果：**

- ✅ 兩個檔案內容完全相同（可使用檔案比對工具）

---

### 測試案例 4：錯誤字元處理 ⚠️

**目的：** 驗證兩種模式對無法轉換字元的處理方式一致

**前置條件：**

1. 準備包含特殊符號或無法轉換字元的文字（例如：emoji 😀 或罕見符號）

**測試步驟：**

1. **內建模式**：轉換包含特殊字元的文字
2. 記錄錯誤訊息或無效字元清單
3. **外部工具模式**：轉換相同文字
4. 記錄錯誤訊息或無效字元清單

**預期結果：**

- ✅ 兩種模式都能正確識別無法轉換的字元
- ✅ 錯誤訊息格式一致
- ✅ 無效字元清單內容相同

---

### 測試案例 5：自訂詞庫測試 📚

**目的：** 驗證兩種模式都能正確載入和使用自訂詞庫

**前置條件：**

1. 準備一個自訂詞庫檔案（如果有的話）
2. 準備包含詞庫中詞彙的測試文字

**測試步驟：**

1. 在轉換對話框中選擇自訂詞庫檔案
2. **內建模式**：轉換包含詞庫詞彙的文字
3. **外部工具模式**：轉換相同文字

**預期結果：**

- ✅ 兩種模式都能正確套用詞庫
- ✅ 詞彙的點字轉換結果一致

---

### 測試案例 6：大型文件轉換 📄

**目的：** 驗證兩種模式處理大型文件的穩定性

**前置條件：**

1. 準備一個較大的文字檔案（建議 500-1000 行）

**測試步驟：**

1. **內建模式**：轉換大型文件
2. 觀察記憶體使用情況（可使用工作管理員）
3. 記錄轉換時間
4. **外部工具模式**：轉換相同文件
5. 觀察記憶體使用情況
6. 記錄轉換時間

**預期結果：**

- ✅ 兩種模式都能成功轉換
- ✅ 應用程式不會當機或記憶體洩漏
- ✅ 記憶體在轉換完成後能正常釋放

---

### 測試案例 7：連續轉換穩定性 🔄

**目的：** 驗證連續多次轉換不會造成記憶體洩漏或效能下降

**測試步驟：**

1. **內建模式**：連續轉換 5-10 次（不關閉應用程式）
2. 觀察記憶體是否持續增長
3. **外部工具模式**：連續轉換 5-10 次
4. 觀察記憶體是否持續增長

**預期結果：**

- ✅ 記憶體保持穩定，沒有明顯持續增長
- ✅ 轉換速度沒有明顯下降
- ✅ 應用程式保持穩定

---

## 測試環境資訊

- **作業系統**：Windows 11
- **測試日期**：2025-11-25
- **建置組態**：Debug
- **.NET 版本**：10.0

---

## 疑難排解

### 問題：找不到 Txt2Brl.exe

如果外部工具模式報錯找不到 `Txt2Brl.exe`，請檢查：

```powershell
# 檢查 Txt2Brl.exe 是否存在
ls output\EasyBrailleEdit\Debug\net10.0-windows10.0.17763.0\Txt2Brl.exe
```

如果不存在，需要確認建置時 PostBuild 事件是否正確執行。

### 問題：組態檔在哪裡？

首次執行應用程式後，`AppConfig.ini` 會自動產生在應用程式執行目錄：

```text
output\EasyBrailleEdit\Debug\net10.0-windows10.0.17763.0\AppConfig.ini
```

---

## 測試結果摘要

### 測試狀態

- ✅ 測試案例 1：內建模式基本轉換 - **通過**
- ✅ 測試案例 2：外部工具模式基本轉換 - **通過**
- ✅ 測試案例 3：結果一致性比較 - **通過**
- ⚠️ 測試案例 4：錯誤字元處理 - **未測試（可選）**
- ⚠️ 測試案例 5：自訂詞庫測試 - **跳過（無自訂詞庫）**
- ⚠️ 測試案例 6：大型文件轉換 - **未測試（可選）**
- ⚠️ 測試案例 7：連續轉換穩定性 - **未測試（可選）**

### 核心功能驗證

**雙模式點字轉換功能已完成基本驗證：**

1. ✅ 內建模式可正常轉換點字
2. ✅ 外部工具模式可正常轉換點字
3. ✅ 兩種模式可透過 `AppConfig.ini` 順利切換
4. ✅ 轉換結果正確且一致

### 備註

測試案例 4-7 為進階驗證項目，可視專案需求選擇性執行。核心雙模式轉換功能已通過基本驗證。
