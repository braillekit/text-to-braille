# 易點雙視版本發布說明

## v5.0.0 (2025-12-01)

- 新增：線上使用手冊，網址為 <https://braillekit.github.io/text-to-braille/>。
- 新增：即時預覽功能，可以在編輯時即時預覽轉換點字的結果。
- 新增：`Ctrl+F5` 直接轉點字（不顯示選項對話窗）。[#69](https://github.com/braillekit/text-to-braille/issues/69)
- 新增：雙模轉點字，預設採用 in-process 模式，速度更快。[#66](https://github.com/braillekit/text-to-braille/issues/66)
- 改善：效能提升、記憶體洩漏問題。
- 作業系統：不再支援 Windows 8 或更舊的作業系統，請使用 Windows 10。
- 執行環境：.NET 10。

## v4.2.4 (2024-01-12)

- 修正：印表機名稱沒有自動保存於設定檔，導致每次列印時都要選擇印表機。
- 改進：使用介面的字型從原本的 新細明體 改為 微軟正黑體。
- 升級第三方套件。

## v4.2.3 (2023-08-29)

- AppConfig.ini 的 [Braille] 區段增加一個參數：

   UseUpperPositionForNumbers = false ; 數字一律使用上位點。v4.2.1 (2023-9-1) 之後開始支援此選項。
   把參數值改為 true 即可在轉點字時，令數字一律使用上位點。

- 新增中文標點符號：《 》轉譯為 (126-126 345-345) ，〈 〉轉譯為 (126 345)。
   注意事項：你如果有在 AppConfig.ini 中把 《 》 重新對應為 <書名號>，則會優先採用你的設定。

- 更新相依套件：

  - "Microsoft.VisualStudio.SlowCheetah" Version="4.0.50"
  - "MinVer" Version="4.3.0"
  - "Serilog" Version="3.0.1"

## v4.2.0 (2022-10-10)

- 從 .NET 6 升級至 .NET 7。

## v4.1.4 (2022-3-18)

- 放寬試用版的限制：
   從這個版本開始，試用期間可以處理最多 5 頁的文字，列印的頁數限制也是 5 頁。
   一旦超過試用期，則只能處理 1 頁的文字。

## v4.1.0 (2022-2-7)

- 重要！開啟雙視檔案時，不再支援 v1.0 時期的 .BTX 二進位格式。
- 技術變更：升級至 .NET 6.0。

## v4.0.0-beta (2021-1-24)

- 技術變更：升級至 .NET 5.0。

## v3.18.0 (2019-12-3)

- 列印對話窗增加參數：「每一方點字的列印寬度」，預設值為 24。
   此參數會用來計算每個明眼字的字寬（會影響字距），因為列印時，明眼字和點字
   必須對齊。
- 列印對話窗增加參數：「每一列明眼字的高度」，預設值為 40.0975。
   此參數可用來調整每一列明眼字所佔據紙張的高度（即字高加上列距）。

## v3.16.0 (2019-08-26)

- 修正問題 #4：在雙視編輯視窗修改點字之後，開啟［檢視 > 明眼字］顯示的
   內容卻是舊的。
- 更新第三方函式庫至新版本。

## v3.15.9 (2019-01-23)

- 更新第三方函式庫至新版本。
- 調整版本授權檢查規則。

## v3.15.7 (2018-12-26)

- 修正：按住 Ctrl+X 或者 Ctrl+Delete 不放，快速產生一連串剪下或刪除的操作，
   會造成 "索引超出範圍" 的錯誤。

## v3.15.6 (2018-12-21)

- 增加符號：△ （1246 2345 點）
   點字規則：前後均空方。
- 增加符號：↔ （246 25 25 135 點）
   點字規則：前後均空方。
- 雙視編輯視窗現在可以按 Ctrl+Shift+V 把剪貼簿的點字貼上至行尾（或者在儲存
   格上點滑鼠右鍵，也可以在選單中找到此功能）。
- 列印明眼字時，現在可以指定列印份數。（列印點字的功能則仍只能列印一份）

## v3.15.3 (2018-12-17)

- 雙視編輯視窗的插入文字視窗，現在也可以將標籤套用至選取文字。
- 雙視編輯時，滑鼠左鍵點擊兩下後，即可修改字(即F4功能)。編輯頁標題時也是。
- 編輯頁標題時，現在也可以使用剪貼簿的功能，包括：複製（Ctrl+C）、剪下
   （Ctrl+X）、貼上（Ctrl+V）。但由於每一列頁標題都是關聯至正文的特定位置，
   所以不可複製和貼上多列，而只能複製同一列中的文字。

## v3.15.2 (2018-12-14)

- 明眼字編輯視窗增加搜尋和取代功能。
   搜尋：Ctrl+F （可一次顯示所有符合的字串）
   取代：Ctrl+H
- 開啟明眼字檔案時，現在一律使用 UTF-8 編碼。
- 製作新的安裝程式。使用此新版安裝程式便可以透過雙擊 .brx 檔案來直接開啟
   易點雙視。  
- 修改授權註冊方式，加入試用期限 30 天。

## v3.14.6 (2018-12-13)

- 應用程式核心元件更新。
   注意：必須更新，否則後續版本在更新時可能會因為無法處理子目錄檔案而失敗。

## v3.14.4 (2018-12-11)

- 修正：在雙視編輯視窗按 WinKey+D 回到 Windows 桌面，或將視窗縮到最小，再
   切回雙視編輯視窗時，垂直卷軸的位置會跑到最頂端。修正後，按 WinKey+D 時已
   經會記住卷軸位置。
- 明眼字編輯視窗增加「復原」與「重做」功能，快捷鍵分別是 Ctrl+Z 和 Ctrl+Y。
- 明眼字編輯視窗左方增加行號（列號），游標加粗並以藍色顯示。
- 明眼字編輯視窗的 <> 標籤文字改以加粗的標楷體顯示，以便和一般文字區分。

## v3.14.0 (2018-11-28)

- 在明眼字編輯視窗中，若先選取了一些文字，然後點擊成對的標籤或符號按鈕
   （例如私名號、書名號、引號等等），以往會以標籤文字或符號取代原先選取
   的文字，現在則會以成對的標籤符號將選取的文字包住。
   示範影片：<https://youtu.be/hfz-OWbCOSQ>
- 雙視編輯視窗增加了「全選」功能。
   操作方式：按 Ctrl+A 或在儲存格上面點滑鼠右鍵，然後點［全選］。
- [技術規格] .NET Framework 版本從 4.7.1 改為 4.7.2。

## v3.13.1 (2018-10-12)

- 修正：自訂片語（phrase.phf）如果是定義一個中文字的注音，會沒有作用。

## v3.13.0 (2018-10-11)

- .NET Framework 版本從 4.5.2 升級至 4.7.1。
- 修正：只要明眼字有包含 "電波"，轉點字時就會出現應用程式錯誤（異常結束）。

## v3.12.3 (2018-08-10)

- 修正：版本更新時，若本機的版本號碼包含非數字的字串，會造成無法更新。
- 修正：版本更新之後不用再重新註冊。
- 為主程式補上圖示（桌面的捷徑才會有貓頭鷹圖示）。
- 更新安裝程式。

## v3.12.0 (2018-07-11)

- 修正：［插入一串文字］視窗的工具列的［箭頭］按鈕沒作用。
- 加入軟體註冊機制。若沒有註冊，將無法列印和輸出雙視文件。

## v3.11.43 (2018-06-23)

- 新增：把雙視文件另存為 HTML 網頁的功能。
  操作方式：從雙視編輯視窗的主選單點「檔案 > 匯出為網頁」。
- 修正：轉點字時，標點符號的前面一律不加空方。
  由於「標點符號」很多，為了避免有特例，所以在 AppConfig.ini 裡面的 [Braille] 區塊增加兩個參數來控制：

  NoSpaceAfterTheseCharacters = 「『“‘…  ;這些字元的右邊一律不自動加空方。
  NoSpaceBeforeTheseCharacters = ，。？！：；、…」』”’）  ;這些字元的左邊一律不自動加空方。

- 修改：拿掉雙視編輯視窗的「匯出文字檔」功能，因為此功能並沒有辦法完美達成最初設計的目的：希望匯出的文字檔可重複轉成點字。原因在於後來增加的一些控制標籤在轉點字的過程中就會丟棄，並沒有保存下來。所以即使能匯出文字檔，也無法保證能夠在轉出一模一樣的點字。使用者若想要查看明眼字，可以透過［檢視 > 明眼字］功能。

## v3.11.40 (2018-06-22)

- 新增：`<URL>` 區塊。用於輸入網址。
- 新增：`<分隔線>`。
- 新增：`<無數符>`。包在此區塊內的數字都不會自動加數符。
- 改進：自動斷行時，加入更多檢查，以確保：
   (1) 左括號和上引號不出現於行尾。
   (2) 右括號和下引號不出現於行首。
- 修正：數學區塊的刪節號應是三個第 3 點。
- 修正：數學區塊的打勾、打圈、打叉符號：
   <數學>（√）。</數學>
   <數學>x，</數學>
   <數學>o。</數學>
   <數學>√。</數學>
- 修正：雙視編輯模式下，按住 Ctrl+Del 不放來快速刪除點字時，常常出現找不到點字物件的情形。  

## v3.11.34 (2018-06-21)

- 新增：雙視編輯的［插入一串文字］功能增加［原書頁碼］與［刪除文
   字］按鈕。
- 新增：主選單的［說明］底下增加［版本修改歷史］。
- 修正：雙視編輯時，「插入一串文字」視窗裡，若點［分數］按鈕，下方會立刻出現紅色訊息提示有錯誤，但沒有顯示任何錯誤訊息。
- 修改：使用原書頁碼標籤 `<P>` 時，如果沒有指定任何頁碼，例如：
  `<P></P>`
  則表示不要原書頁碼（清空原書頁碼）。
- 修改：`<刪>` 區塊裡面的文字一律不加空方，也不加數符。

## v3.11.32 (2018-06-20)

- 修正數學點字，以滿足下列規則：
  (a) 數學區塊的問號跟一般文字的問號是不同的點字。
  (b) 算式裡，只有第一個數字要加數符，但第一個數字左邊若是左括弧，則不加數符。
  (c) 算式之間都不空方，例如 "（ ）x5" 的乘號和 5 之間不加空方。
  (d) 問號跟刪節號之間要空方。
- 修正：數學區塊中的編號項目【 】裡面不應自動加空方。
- 修改：全形水波符號 '～' 的點字改為兩方的 (2 5) 點（跟短破折號一様）。

## v3.11.28 (2018-06-14)

- 增加：`<特殊引號一>` 和 `<特殊引號二>` 標籤。這兩個標籤都會產生一對中文全形的單引號：「」，但是它們的下引號所對應的點字並不相同。
- 修正：全形引號裡面，上引號「的右邊不加空方，下引號」的左邊不加空方。
- 修正：英文全形單引號和雙引號裡面不加額外空方。
- 改善：因殘留多個 `<上位點>` 標籤，導致列印時找不到原書頁碼。
- 修正：雙視編輯的［插入一串文字］功能，如果插入了很長的文字，轉成點字之後超過兩行，網格上面只會顯示兩行的內容，而少了其他行（只是顯示的問題，文件內容是正確的）。
- 其他操作介面的小改進，例如雙視編輯的網格背景顏色加深，以便其他對話窗顯示時更明顯。

## v3.11.24 (2018-06-13)

- 修正：全形方括號【】裡面有數字時，括號後面的數字的數符也被刪除了。現在修正為只有方括號裡面的數字才會移除數符。
- 改善：如果原書頁碼加了多餘的 `<上位點>` 標籤，預覽列印時會略過。

## v3.11.23 (2018-06-12)

- 增加：［比例］按鈕，會固定產生字串 "1:1"。主要是為了避免使用者在輸入比例時，使用了中文全形的冒號。
- 修正：數學區塊的括弧符號的點字。
  測試案例：`<數學>（1cm寬）</數學>`。
- 修正：數學區塊中的全形冒號、上引號、下引號的點字。
- 修正：在某些情況下，執行［段落重整］時會造成程式當掉。
- 改善：如果原書頁碼加了多餘的 `<上位點>` 標籤，預覽列印時會略過。

## v3.11.19 (2018-06-09)

- 新增：雙視編輯模式的「插入一串文字」視窗裡面增加數學符號列。
- 修正：數學區塊 `<數學>` 中的圓括弧（）中間若有數字，數字左邊不加空方。
- 修正：數學點字規則中有關括弧、逗號、分號及句號的處理。
- 改進：執行［段落重整］時，程式會自動偵測可能造成無窮迴圈的狀況。若判定該次段落重整操作可能造成無窮迴圈，程式將中止操作，並提示使用者如何復原該次操作。

## v3.11.14 (2018-06-08)

- 修正：雙視編輯模式的尋找文字功能有時會出錯。

## v3.11.13 (2018-06-07)

- 改進：雙視編輯視窗中，若當前所選取的那行是新的頁標題的開始位置，那麼狀態列就會顯示頁標題的內容。此外，編輯網格的左邊行號也會標示 "標" 來代表那行開始有一個新的頁標題。頁標題編輯視窗的狀態列則會顯示目前選取的頁標題所對應至雙視文件中的行號。
- 修正：表格的中間與右邊界垂直線轉出來的點字不正確。

## v3.11.11 (2018-06-06)

- 新增：「表格橫槓 (10方)」按鈕。此功能可用來插入 10 個表格橫線符號。其中一個可能的用途式做為數學的直式算式的橫線。
- 新增：雙視編輯模式增加一個功能：「移除選取範圍的數符」。快速鍵是 Ctrl+Alt+3。
- 改進：插入表格時，現在可以讓使用者自行指定要幾列、幾欄，以及每
   一欄裡面要有幾個空方；而且明眼字和雙視編輯模式都可以插入表格。
- 改進：修改一些破音字的預設讀音，包括：礦、怎、不、液....等等。
- 改進：「可復原的操作」視窗現在多了一個核取方塊，讓使用者可以自行決定是否總是顯示這個視窗。
- 改進：頁標題編輯視窗開啟和儲存時，會檢查並刪除空的頁標題。
- 改進：頁標題編輯視窗增加更多編輯功能。主要是刪除一行和複製貼上。
- 修正：當自訂片語檔 phrase.phf 裡面有包含重複的字詞時，例如 "什" 和 "什麼"，轉點字會拋錯。
- 修正：編輯雙視文件時，有些殘留的數符會導致插入新字串時發生錯誤。（註：數符 '#' 在編輯時不會顯示在畫面上）
- 修正：在雙視文件中插入頁標題，點了預覽列印按鈕之後，那個新的頁標題會消失。除了修正此問題，加入新頁標題之後，在頁標題清單裡面也會按照顯示順序來重新排序了。
- 修正：執行 段落重整 時，特定情況下會造成程式當掉。

## v3.11.0 (2018-06-04)

- 新增：編輯雙視文件時，現在已經有「復原」和「重做」功能。 「復原」的作用是復原上一次的修改，快速鍵是 Ctrl+Z。 「重做」的作用是取消上一次的「復原」操作，快速鍵是 Ctrl+Y。 注意：只會記住最近十次的修改操作。換句話說，只能復原最近十次以內的修改。此復原數量的限制是由 AppConfig.ini 的參數來控制：

    [BrailleEditor]
    MaxUndoLevel = 10

## v3.10.14 (2018-06-01)

- 新增：雙視編輯視窗的主選單的「檢視」底下增加「重新整理」功能。此功能的作用：把雙視文件的內容重新填入視窗編輯區的表格。註：有時候，因為程式在處理表格的視覺化顯示的地方有些問題，使得某些修改動作（例如刪除文字）而造成編輯區的表格無法正常顯示，此時便可以利用此功能來重新把資料填入表格。
- 新增：雙視編輯視窗的主選單的「工具」底下增加「檢查文件中是否有空標籤」的功能。
- 修正：選取同一行裡面的文字時，若選到沒有點字的區域，複製到剪貼簿時會拋錯。現在已經能偵測並自動修正複製時的選取區域，而且複製到剪貼簿時，會在狀態列提示已經複製個點字或幾行文字到剪貼簿。

## v3.10.11 (2018-05-31)

- 新增：雙視編輯視窗的主選單新增了一個功能："工具 > 移除所有原書頁碼的 # 符號"。此功能會在整個雙視文件中搜尋搜有的原書頁碼，並將多餘的 # 號移除。
- 現在編輯頁標題的時候，也有「插入一串文字」的功能。
- 雙視編輯模式下執行「插入一串文字」時，現在也可以加入頁標題了。
- 修正：雙視編輯視窗的「重新抓取頁標題」功能會把既有的頁標題清除。現在此功能只會把新發現的頁標題加入，不會移除既有的頁標題。
- 修正：在雙視編輯模式下第二次開啟檔案，在編輯文件時會發生錯誤。
- 修正：雙視編輯時，在列首按 Backspace 來執行倒退刪除，應該只將該列銜接至上一列（但實際的結果卻銜接多行，類似「段落重整」）。
- 修正：雙視編輯時，按 Enter 鍵來斷行，如果斷下來的文字都是空白，grid 沒有正常顯示新產生的列。  

## v3.10.4 (2018-05-29)

- 修正：雙視文件若包含 cell 數量為 0 的空行，會在點選儲存格時拋錯。
- 修正：預覽列印時，頁尾的原書頁碼不應該包含橫槓符號。

## v3.10.2 (2018-05-28)

- 雙視編輯的複製與貼上功能，現在已經可以一次複製和貼上多行。
- 修正：雙視編輯模式下開啟 BRX 檔案，頁標題會不見。

## v3.10.0 (2018-05-25)

- 雙視編輯模式增加剪貼簿的複製（Ctrl+C）和貼上（Ctrl+V）功能。注意：每次只能複製同一行裡面的文字，而不可跨行區塊選取。
- 雙視編輯模式的主選單新增「編輯 > 文件屬性」功能，可設定匯出點字或列印時的起始頁碼。此設定值會在儲存雙視檔案時一併保存，這樣以後在輸出點字檔案時，就可以直接使用先前設定的起始頁碼，不用再額外指定。
- 雙視編輯模式的主選單新增「檢視 > 點字」功能，可用來查看目前編輯的雙視文件的所有點字內容（以 ASCII 字元顯示，而不是點字圖案）。此功能類似「匯出點字檔」，差別在於此功能適合快速查看，無須存檔。
- 雙視編輯模式的主選單新增「檢視 > 明眼字」功能，可用來查看目前編輯的雙視文件的所有明眼字。

## v3.9.0 (2018-05-24)

- 修正：雙視編輯模式下，「插入一串文字」之後，如果插入的文字包含標籤（例如 <點譯者註>），會造成游標定位錯誤，以致於影響後續的修改操作。
- 「匯出點字檔」功能現在可以指定輸出的起始頁碼（點字頁碼）。
- 雙視編輯視窗下方增加狀態列，以顯示當前游標所在的文字。

## v3.8.3 (2018-05-23)

- 修正：「結束」標籤轉成點字時，結果是空的（沒有任何內容）。
- 修正：斷行時如果正好斷在空白處，會造成空白被移除。

## v3.8.2 (2018-05-22)

- 雙視編輯模式下，新增「插入一串文字」功能。以往把明眼字文件轉成點字之後，在雙視編輯模式下，每次只能插入一個字。現在有了這個新功能，在雙視編輯模式下插入一串文字就輕鬆多了。

## v3.8.1 (2018-05-21)

- 新增「匯出點字檔」功能。此功能可以在雙視編輯視窗的主選單的「檔案」選單底下找到。匯出點字檔的副檔名是 .BRL。用途：供超點的 WCBE（中英文點字編輯器）讀取，以便進行點字校對工作。因此，列印時輸出的點字檔案，副檔名恢復成原本的 ".BRP"。
- 修正：雙視編輯模式下，插入一個新的中文字時，若該中文字占三方點字，則當回到雙視編輯模式時，必須按兩次 Tab 鍵才能將游標移至下一個儲存格。修正後，只要按一次 Tab 鍵便可移動游標。
- 提供批次轉檔工具 BTX2BRX。用途：將易點雙視 1.x 版的雙視檔案 (.BTX) 轉換成 3.x 版的 .BRX 格式。此工具可把多個 .BTX 檔案一次全部轉換成 .BRX，同時也解決了易點雙視 2.x 版讀取 .BTX 檔案時會遺漏頁標題的 bug。

## v3.7.8 (2018-05-11)

- 修正：雙視編輯時，按 Backspace 有時會變成刪除目前的字。
- 修正：雙視編輯時，按左方向鍵應該要以「字」為單位往左移動一個位置，而不是往左移動一個儲存格。
- 修正：雙視編輯時，「段落重整」在某些情況下會沒處理到一些行。

## v3.7.7 (2018-05-10)

- 新增「段落重整」功能。在雙視編輯模式下，點滑鼠右鍵可找到此功能。或者按快速鍵: Ctrl+Shift+F。

## v3.7.6 (2018-05-08)

- 改進：現在全形水波符號 '～' 也可以轉點字了。點位跟半形水波符號相同。
- 改進：支援更多種破折號字元。
- 新增規則：轉點字時，私名號、書名號不能單獨在行尾。
  說明：轉點字時，私名號和書名號的前導字元（╴╴ 和 ﹏﹏）如果正好位於行尾，便會自動折到下一行，以免前導字元跟人名或書名被拆開放在不同行。
- 新增規則：轉點字時，破折號、刪節號不能單獨在行首。
  說明：轉點字時，破折號、刪節號如果正好被編排在行首，便會把上一行末尾的文字折下來跟破折號或刪節號排在一起。

## v3.7.5 (2018-05-07)

- 修正原書頁碼：不用輸入 # 號，但轉成點字時要自動加數符。
- 修改副檔名：.brlj 改為 .brx。 .brp 改為 .brl。

## v3.7.4

- 增加：`<上表格線1>` 與 `<下表格線1>`。
  這兩個標籤是單行標籤，會轉點字時會自動轉成 40 方的點字。
- 增加：`<上表格線2>` 與 `<下表格線2>`。
  這兩個標籤是單行標籤，會轉點字時會自動轉成 40 方的點字。
- 增加：`<選項>`。用來處理 "ㄅ." 的特殊點字規則。
  範例：`<選項>ㄅ.</選項>`
- 增加：目錄（目錄）的點點符號（類似刪節號，但呈現方式不同）。
- 增加：<上位點>。包在此標籤內的數字都會使用上位點。

## v3.6.0

- 大幅修改核心程式碼，以完整實現「匯出文字檔」功能。

## v3.5.0

- 修改：`<P> </P>` 裡面的數字一律使用上位點，且不加數符。
  設定值：AppConfig.ini 裡面的 UseUpperPositionForOrgPageNumber。
  若此設定為 true，代表一律使用上位點；
  若為 false 則頁碼會加數符，且不使用上位點。
- 修正：原書頁碼若使用羅馬數字，列印時沒有印出來。
- 雙視編輯視窗加入一個實驗性質的功能：匯出文字檔。
  此功能可將目前雙視編輯的文件內容取出明眼字的部分，並儲存成文字檔。
  設計此功能的用意是讓使用者在辛苦修改好雙視文件之後，能夠把它的純文
  字的部分保存下來。將來可以繼續修改此文字檔案，並再次轉成雙視文件。
  注意：此功能仍在實驗階段，若有請提供反饋意見。
- 原本點［刪除文字］按鈕時，會在文字編輯區產生 "`<刪>▲▼</刪>`"。現在
  改為 "`<刪></刪>`"，然後等到轉點字時才產生刪除符號 "▲▼"。

## v3.4.0 (2018-04-27)

- 私名號與書名號後面接任何標點符號不加空方。
- 點譯者註一律使用左右括弧包起來，且括弧內的文字前後不加空方（括弧跟文字緊鄰）。這裡的文字包括中文字與標點符號、英文、數字等等。
- 雙視檔案 .brlj 格式變動；保存更多屬性，檔案也會比先前的版本大一些。
  注意：請用此新版程式來載入以前的雙視檔案，若發生錯誤，請儘快反映。

## v3.2.3 (2018-04-25)

- 圓括號的後括號接標點符號不用空方。這裡的標點符號包括以下全形符號：句號、逗號、分號、冒號、問號、驚嘆號、引號、刪節號、括弧....等等。
  範例："（晴朗）。" 的右括弧與句號之間不應加空方。
- 修正：「 關於」視窗裡面顯示的版本號碼只顯示到次版號（例如
    3.2.0.0），而無法顯示修正版號，例如 3.2.1。
- 非功能面的修正。整理內部程式碼：EnglishBrailleTable.xml（修改數字9、字母 I 和 T 的點字代碼）。

## v3.2.1 (2018-04-24)

- 修正：句號、問號、驚嘆號後面接後括號（包括圓形括弧、方括弧、大括弧、下引號等封閉右邊的福號）不用空方。
- 命令列工具 txt2brl 改變用法。增加選項 -i、-o、--stdin。

## v3.2.0 (2018-04-23)

- 台北市視障者家長協會專用版本。
- 修正：btx 視窗在挑選點字時，有一個符號是空的。

## v3.1.0 (2018-04-20)

- 把所有選項和參數都統一存放於一個檔案：AppConfig.Ini。
- 修改底層架構，重整程式碼結構。

---

## v2.9.0 (2018-04-15)

解決在 Windows 10 環境中，由於微軟的注音輸入法改版而無法轉換中文點字的問題。從這個版本開始，易點雙視已經內建中文注音詞庫，不再倚賴微軟輸入法。

## v2.8.2 (2018-03-31)

- 修正 bug：輸出點字檔案（.brp）時，abcd 要直接是小寫。
  詳見 <https://github.com/huanlin/EasyBrailleEdit/issues/7>

- 修正自動更新檔案清單裡面的 Huanlin.DLL 檔名大小寫不一致而造成自動更新失敗的問題。

- 應用程式內部改善：增加 logging 機制（Serilog）、自動更新時採用非同步下載檔案的寫法。

## v2.8.0 (2018-02-18)

- 開放原始碼。版本名稱加上「社群版」，移除所有試用版限制與註冊檢查。

## v2.7.2 (2016-11-07)

- 修正版本自動更新功能失效的問題（更換檔案伺服器）。

## v2.7.1 (2016-11-03)

- 轉換點字的對話窗現在提供了兩個選項可供使用者設定：
  (1) 每頁幾列 （對應至 Settings.xml 中的 LinesPerPage）
  (2) 每列幾方 （對應至 Settings.xml 中的 CellsPerLine）

- 改變列印點字時的跳頁方式，以便支援 ET Trident 點字印表機。
  說明：原本使用換行符號來讓點字印表機跳頁，現在使用跳頁符號。
  如欲改回舊版的跳頁方式，可修改 Settints.xml 檔案，將參數 PrintBrailleUseNewLineForPageBreak 設定成 true。

- 列印點字時，增加一個選項：「在文件結尾輸出跳頁符號」。
  說明：通常此選項不用打勾。如果你的點字印表機總是少印了最後一頁，便可
  嘗試將此選項打勾。

- 點字存檔時已經不支援 1.x 版的格式（開啟點字檔案時仍然支援舊版格式）。

## v2.7.0 (2015-12-21)

- 執行平台從 .NET 4.0 升級至 .NET 4.5.2。

## v2.6.1 (2015-07-19)

- 主選單的 [檔案] 之下增加 [設定預覽明眼字的印表機]。
- 應用程式內部整理、元件更新。

## v2.3.14 (2011-06-25)

- 修正 v2.3 無法轉點字的程式錯誤（導因於版本更新伺服器上的檔案版本不正確）。

## v2.3.11 (2011-06-14)

- .NET Framework 從 2.0 升級至 3.5。
- 程式內部的小調整。

## v2.0

- 解決 Windows XP SP3、Windows Vista、Windows 7 和 Windows 2008 作業系統的相容性問題。

  **說明：**由於微軟新注音程式介面的變動，如果您在 Windows Vista 或 Windows 7 執行易點雙視 1.x 版，會發生中文字無法轉換成點字的情形。2.0 版已經解決此問題。

- 雙視檔案有新的檔案格式，副檔名為 .BRLJ。為了讓您可以繼續處理原先儲存的檔案，舊版的 .BTX 檔案仍可以繼續使用，但由於 .BTX 檔案的處理速度比較慢，且容易產生新舊版本資料不相容的問題，因此建議不要再使用。未來將會逐漸淘汰 .BTX 檔案，並全面改用 .BRLJ 格式的雙視檔案。

- 系統內部異動：自動更新的網址與 v1.0 不同，新版為 `..../UpdateWeb/EBE2`。

## v1.14 (2009-12-04)

- 修正
  
  - 列印點字時，若選擇輸出至檔案，檔案內容的某些字元與「超點」觸摸顯示器不
      符。修改的部分如下：

      1.全部的英文改成小寫(  A B C D  改成  a b c d)。
      2.  ^  符號改成 ~ 符號。
      3.  @  符號改成 ` 符號。
      4.  [  符號改成 { 符號。
      5.  ]  符號改成 } 符號。
      6.  \  符號改成 | 符號。

- 改進

  - 當程式沒有偵測到注音輸入法和新注音輸入法時，原本會強制關閉程式，現在只在應用程式視窗的下方狀態列顯示提示訊息。因為偵測輸入法的功能在 Window 7 和 Windows Server 2008 可能無法正常運作。

## v1.13 (2009-06-25)

- 改進

  - 關閉點字編輯視窗時，如果尚未儲存，原本會直接關閉，現在會詢問是否儲存。

- 修正

  - 在編輯點字時，如果儲存格的內容沒有文字，只有點字，在按 Enter 鍵斷行時，除該儲存格的資料會不見。
  - 有些情況因為程式本身的瑕疵而造成儲存的點字資料（.btx）有空白列時，將點字資料載入之後，會造成資料窗格中對應的那一列無法操作。現在程式載入點字
      資料時，會額外檢查並剔除有無效的資料列，以確保資料之正確性。
  - 編輯點字的頁標題時，刪除點字會出現嚴重錯誤。

## v1.12 (2009-06-24)

- 新增
  
  - 編輯明眼字時，在編輯區域點滑鼠右鍵會出現快顯功能表，提供基本的剪貼簿功能。
  - Braille.ini 增加 AutoIndentNumberedLine 參數，用來控制：當以 # 開頭的編號項目文字折行時，是否要自動從 # 的位置再往內縮一方。若為 True，即表示要自動內縮。預設為 False。詳細說明請參考： <http://innoobj.blogspot.com/2009/06/faq_23.html>

- 修正
  
  - 編輯點字時，刪除和修改功能會出現嚴重錯誤。

## v1.11 (2009-06-23)

- 修正
  
  - 編輯點字時，第 40 方無法顯示。
  - 詞庫檔只固定使用 Phrase.phf。修正後可以指定多個詞庫檔。

## v1.10 (2009-06-22)

- 增加
  
  - 箭頭符號：↖ ( 1246 45 246 25 25)
  - 箭頭符號：↗ (1246 45 25 135)
  - 箭頭符號：↙ (1246 56 246 25 25)
  - 箭頭符號：↘ (1246 56 25 25 135)
  - 上升語調：[↗] (6 25)
  - 下降語調：[↘] (25 3)
  - @ (4)
  - & (12346)
  - 水波 ~：(36 36)
  - 音標括號：[] (25 25  25 25)
    **註：**插入音標的對話窗中，增加了一個「自動加音標括號」的核取方塊，預設為勾選，表示在插入音標時，會自動為您加上音標括號。
  
- 修改
  
  - 精讀號：◎ (246 135)
  - 錢字符號：$ 和＄ (256)
  - 音標：θ (1456)
  - 增加直向工具列，把部分符號搬移到直向工具列。
  
- 修正

  - 原本的題號【1】只能一位數（也就是最多到【9】），現在可以多位數。
  - 雙視編輯視窗的儲存格標題數字編號如果是兩位數，有時候只會顯示一位數。
      這是因為字型太大，導致儲存格放不下的緣故，已將字型縮小一號。
  - 向左和向右箭頭前面不加 1246 點。

## v1.9 (2009-06-14)

- 增加
  
  - 每當更新完成時，顯示版本變更記錄（ChangeLog.txt）。
  
- 修正
  
  - 解決智慧型判斷詞彙注音失效的問題，同時支援新注音 2007。
  - 解決自訂詞庫失效的問題。
